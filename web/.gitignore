# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Playwright e2e output (screenshots, video, traces) and HTML report
e2e-results
test-results
playwright-report

# Nightly full-scale suite (issue #29) — its own output dirs, see
# playwright.config.ts's HTP_K8S_E2E_SUITE branch.
e2e-nightly-results
playwright-nightly-report

# go-task local cache (from running `task` against Taskfile.yml)
.task

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
